/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package tallerVoraces;

import java.util.HashMap;
import java.util.Iterator;
import java.util.LinkedList;


/**
 *
 * @author Santitigaga
 */
public class Dijkstra {
    
            public void dijkstra(HashMap mapa,String origen,String Destino) {
                String[][]matriz=new String[mapa.size()+1][mapa.size()+1];
   
            }
        public boolean factible(String x[]) {
        return false;
    }
        public  void llenadoTabla(HashMap <String,LinkedList<String>> c){
                Iterator it = c.keySet().iterator();
        while (it.hasNext()) {
            String key = (String) it.next();
            for (int i = 0; i < 10; i++) {
                
            }
            }
        }
        

    public String[] seleccion(HashMap c) {
        String s[] = new String[2];
        return s;
    }

    public boolean solucion(HashMap s) {
        return false;
    }
}
